language: node_js
node_js: 8
cache:
  directories:
    - node_modules
before_script:
  - npm run lint
  - npm run test:unit
script:
  - npm run build
deploy:
  local-dir: dist
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
